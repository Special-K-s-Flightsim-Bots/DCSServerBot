{
  "color": "blue",
  "title": "Active Campaigns",
  "elements":
  [
    {
      "type": "SQLTable",
      "params": {
        "sql": "SELECT name as \"Name\", '<t:' || EXTRACT(EPOCH FROM start)::INTEGER || '>' AS \"Start\", COALESCE('<t:' || EXTRACT(EPOCH FROM stop)::INTEGER || '>', '-') AS \"Stop\" FROM campaigns WHERE COALESCE(stop, NOW()) >= NOW()"
      }
    }
  ]
}
