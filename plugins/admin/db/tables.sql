CREATE TABLE IF NOT EXISTS bans (ucid TEXT PRIMARY KEY, banned_by TEXT NOT NULL, reason TEXT, banned_at TIMESTAMP NOT NULL DEFAULT NOW(), banned_until TIMESTAMP NOT NULL DEFAULT TO_DATE('99991231','YYYYMMDD'));
