{
  "color": "blue",
  "title": "Current Errors",
  "input": [
    {
      "name": "limit",
      "default": 10
    }
  ],
  "elements":
  [
    {
      "type": "SQLTable",
      "params": {
        "sql": "select version as \"Version\", filename || ' (line ' || lineno || ')' as \"File\", count(distinct guild_id) as \"Guilds\" from cloud_errors where (version, filename, lineno, message) not in (select version, filename, lineno, message from cloud_fixes) group by 1, 2 order by 1 desc, 3 desc",
        "inline": true,
        "no_data": "There are no errors logged."
      }
    }
  ]
}