schema;master_schema:
  type: map
  nullable: false
  mapping:
    enabled: {type: bool, nullable: false}
    wipe_stats_on_leave: {type: bool, nullable: false}
    squadrons:
      type: map
      nullable: false
      mapping:
        self_join: {type: bool, nullable: false}
        persist_list: {type: bool, nullable: false}
        highscore:
          type: map
          nullable: false
          mapping:
            params:
              type: map
              nullable: false
              mapping:
                limit: {type: int, nullable: false, range: {min: 3, max: 20}}
    highscore: {type: any, nullable: false} # todo
    # Allow deep nesting for Node/Instance declaration
    regex;(.+):
      include: 'master_schema'

type: map
nullable: false
mapping:
  DEFAULT:
    include: 'master_schema'
  commands:
    include: 'commands_schema'
  regex;(.+):
    include: 'master_schema'
