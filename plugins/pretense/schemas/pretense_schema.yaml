schema;master_schema:
  type: map
  nullable: false
  mapping:
    update_interval: {type: int, range: {min: 60}, nullable: false}
    json_file_path: {type: str, nullable: false, range: {min: 1}}
    channel: {type: int, nullable: false}

type: map
nullable: false
mapping:
  DEFAULT:
    include: 'master_schema'
  commands:
    include: 'commands_schema'
  regex;(.+):
    include: 'master_schema'
    regex;(.+):
      include: 'master_schema'
