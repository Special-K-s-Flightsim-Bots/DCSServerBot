schema;master_schema:
  type: map
  nullable: false
  mapping:
    packages:
      type: seq
      nullable: false
      sequence:
        - type: map
          nullable: false
          mapping:
            name: {type: str, required: true, nullable: false, range: {min: 1}}
            version: {type: text, required: true, nullable: false}
            source: {type: str, enum: ['SavedGames', 'RootFolder'], required: true, nullable: false}
            repo: {type: str, nullable: false, range: {min: 1}}

schema;default_schema:
  type: map
  nullable: false
  mapping:
    SavedGames: {type: str, required: true, nullable: false, range: {min: 1}}
    RootFolder: {type: str, required: true, nullable: false, range: {min: 1}}

type: map
nullable: false
mapping:
  DEFAULT:
    include: 'default_schema'
  regex;(.+):
    include: 'master_schema'
    regex;(.+):
      include: 'master_schema'
