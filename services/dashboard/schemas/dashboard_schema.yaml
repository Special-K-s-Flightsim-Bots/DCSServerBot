type: map
nullable: false
mapping:
  DEFAULT:
    type: map
    nullable: false
    mapping:
      servers:
        type: map
        nullable: false
        mapping:
          hide_remote_servers: {type: bool, nullable: false}
          background: {type: str, nullable: false, range: {min: 1}}
          border: {type: str, nullable: false, range: {min: 1}}
      regex;(header|nodes|log):
        type: map
        nullable: false
        mapping:
          background: {type: str, nullable: false, range: {min: 1}}
          border: {type: str, nullable: false, range: {min: 1}}
