type: map
mapping:
  DEFAULT:
    type: map
    mapping:
      servers:
        type: map
        mapping:
          hide_remote_servers: {type: bool}
          background: {type: str}
          border: {type: str}
      regex;(header|nodes|log):
        type: map
        mapping:
          background: {type: str}
          border: {type: str}
