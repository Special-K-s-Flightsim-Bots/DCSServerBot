type: map
mapping:
  DEFAULT:
    type: map
    mapping:
      hide_remote_servers: {type: bool}
      regex;(header|servers|nodes|log):
        type: map
        mapping:
          background: {type: str}
          border: {type: str}
